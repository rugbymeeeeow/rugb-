# print("Ниже введите имя и возраст четверых человек")
s=[] #пустой список ради списка
a=[] #список добавился в процессе
i=1 #счётчик, чтобы не писать name1 age1
while i<5:
  name = input("Введите имя:\n")
  age = int(input("Введите возраст:\n" ))
  a += name,age
  s += [a]
  i += 1
  a = []

for h in s:
  print("Вы ввели:\n",h[0],":",h[1])
  n=0
  for h in s:
    if h[1]>n:
    n=h[1]
    b=h[0]

print("Самого старшего из вашего списка зовут:" ,b)
k=input("Введите имя, чтобы проверить есть ли такой человек в вашем списке:\n")
if k in name:
  print("Такой пользователь есть")
else:
  print("Увы, такого пользователя в в списке нет")
